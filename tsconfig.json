{
  "extends": "@tsconfig/svelte/tsconfig.json",

  "include": ["src/**/*"],
  "exclude": ["node_modules/*", "__sapper__/*", "public/*"],

  "compilerOptions": {
    "types": [ "@types/wicg-file-system-access"] // Adds the FileSystemAccess API types
   }
}
